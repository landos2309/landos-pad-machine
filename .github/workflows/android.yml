name: Build Android APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Create Config
        run: echo '{"appId": "com.landos.pad", "appName": "LandosPad", "webDir": "www", "bundledWebRuntime": false}' > capacitor.config.json
      - name: Build and Sync
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap add android --no-interactive || echo "Already added"
          npx cap sync
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          
